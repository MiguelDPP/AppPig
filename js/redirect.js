var redirectSheet = document.getElementById("redirectSheet"),
    redirectUser = document.getElementById("redirectListUser");

if (redirectSheet != null) {
    redirectSheet.click();
} else if (redirectUser != null) {
    redirectUser.click();
}